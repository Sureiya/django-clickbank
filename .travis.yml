language: python
python:
  - "2.7"
# command to install dependencies
install:
  - "pip install -r test-requirements.txt"
# command to run tests
script:
  - "pip install -r test-requirements.txt"
  - "python runtests.py"
